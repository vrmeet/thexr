defmodule Thexr.FuncCoreTest do
  use Thexr.DataCase
  alias Thexr.FuncCore

  # setup do
  # spec =
  #   %{
  #     rules: [
  #       %{msg: "player_moved", op: "set"}
  #     ]
  #   }
  #   |> Jason.encode!()

  # state = FuncCore.new(spec)
  # %{state: state}
  # end

  # test "can new it", %{state: state} do
  #   IO.inspect(state)
  # end

  # test "can receive a message", %{state: state} do
  #   new_state = FuncCore.handle_msg(state, {"player_moved", ["bobby", 1]})
  #   IO.inspect(new_state)
  # end
end
